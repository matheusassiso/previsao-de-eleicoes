---
title: "Modelo de previsao eleitoral presidencial"
subtitle: "Base, variaveis, analise exploratoria e trilha de modelagem para 2026"
author: "Codex"
date: today
format:
  html:
    toc: true
    toc-depth: 2
    theme: cosmo
    code-fold: true
execute:
  warning: false
  message: false
---

```{python}
#| label: setup
from pathlib import Path
import math
from html import escape

import matplotlib.pyplot as plt
import pandas as pd
from IPython.display import HTML, display

ROOT = Path("..")
RAW = ROOT / "data" / "raw"
PROCESSED = ROOT / "data" / "processed"

plt.rcParams.update({
    "figure.figsize": (9, 5),
    "axes.spines.top": False,
    "axes.spines.right": False,
    "axes.grid": True,
    "grid.alpha": 0.2,
})

BLUE = "#2F6F9F"
GREEN = "#2F6F5E"
ORANGE = "#C96F2D"
GRAY = "#A0A7B3"
DARK = "#1F2933"

display(HTML("""
<style>
.executive-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
  gap: 12px;
  margin: 1rem 0 1.2rem;
}
.metric-card {
  border-left: 4px solid #2F6F9F;
  background: #f7f9fb;
  padding: 0.8rem 0.9rem;
}
.metric-label {
  color: #5b6770;
  font-size: 0.86rem;
  margin-bottom: 0.2rem;
}
.metric-value {
  color: #1F2933;
  font-size: 1.45rem;
  font-weight: 700;
  line-height: 1.1;
}
.metric-note {
  color: #5b6770;
  font-size: 0.82rem;
  margin-top: 0.25rem;
}
.callout-box {
  border-left: 4px solid #C96F2D;
  background: #fff8f2;
  padding: 0.9rem 1rem;
  margin: 1rem 0;
}
.callout-box strong {
  color: #1F2933;
}
</style>
"""))

def read_csv(path):
    return pd.read_csv(path) if path.exists() else pd.DataFrame()

def pct(value):
    return f"{float(value):.1f}%".replace(".", ",")

def metric_card(label, value, note=""):
    return f"""
    <div class="metric-card">
      <div class="metric-label">{escape(str(label))}</div>
      <div class="metric-value">{escape(str(value))}</div>
      <div class="metric-note">{escape(str(note))}</div>
    </div>
    """

CURRENT_SCENARIO_LABEL = "Primeiro turno 2026 - oficial pos-TSE"

def current_scenario(data):
    if data.empty or "cenario" not in data:
        return data
    scenario = data[data["cenario"].astype(str).eq(CURRENT_SCENARIO_LABEL)].copy()
    if not scenario.empty:
        return scenario
    recent = data[
        data["cenario"].astype(str).str.contains("oficial pos-TSE|agosto/setembro", na=False)
        & ~data["cenario"].astype(str).str.contains("Marcal|Marçal", case=False, na=False)
    ].copy()
    return recent if not recent.empty else data.copy()

def confidence_label(score):
    if score >= 80:
        return "alta"
    if score >= 60:
        return "media"
    return "baixa"

polls = read_csv(PROCESSED / "pesquisas_validos.csv")
raw_polls = read_csv(RAW / "pesquisas.csv")
elections = read_csv(RAW / "eleicoes.csv")
fundamentals = read_csv(RAW / "fundamentos_eleicao.csv")
forecasts = read_csv(PROCESSED / "previsoes.csv")
ensemble_forecast = read_csv(PROCESSED / "previsao_ensemble.csv")
forecast_history = read_csv(PROCESSED / "previsoes_historico.csv")
bayesian_forecast = read_csv(PROCESSED / "previsao_bayesiana_dinamica.csv")
backtest = read_csv(PROCESSED / "backtest_modelos.csv")
backtest_janelas = read_csv(PROCESSED / "backtest_janelas.csv")
backtest_vivo = read_csv(PROCESSED / "backtest_previsao_viva.csv")
rolling = read_csv(PROCESSED / "rolling_comparacao.csv")
rolling_summary = read_csv(PROCESSED / "rolling_resumo_modelos.csv")
rolling_2026 = read_csv(PROCESSED / "rolling_previsao_2026.csv")
quality = read_csv(PROCESSED / "qualidade_institutos.csv")
house_effect = read_csv(PROCESSED / "efeito_casa_instituto_candidato.csv")
source_audit = read_csv(PROCESSED / "auditoria_fontes.csv")
recent_audit = read_csv(RAW / "auditoria_2026_recente.csv")
candidates_2026 = read_csv(RAW / "candidatos_2026_auditoria.csv")
```

## Leitura executiva

Este projeto deve funcionar como um nowcast eleitoral: a cada nova pesquisa, o modelo atualiza a estimativa do primeiro turno em votos validos e preserva a previsao antiga para acompanhar a evolucao da corrida.

A primeira versao deve privilegiar clareza e backtest. Modelos sofisticados entram apenas depois de a base historica estar limpa e de vencerem um baseline simples.

O teste mais importante nesta versao e cronologico: treinar apenas com eleicoes anteriores, prever a proxima e comparar com o resultado real. Assim, a avaliacao imita melhor o que aconteceria antes de uma eleicao nova.

```{python}
#| label: resumo-executivo
cards = []
if not polls.empty:
    unique_polls = polls.drop_duplicates(["ano_eleicao", "instituto", "data_publicacao", "cenario"]).shape[0]
    latest_date = pd.to_datetime(polls["data_publicacao"], errors="coerce").max()
    cards.append(metric_card("Linhas candidato-pesquisa", f"{len(polls):,}".replace(",", "."), "uma pesquisa vira varias linhas"))
    cards.append(metric_card("Pesquisas/cenarios unicos", f"{unique_polls:,}".replace(",", "."), "ano-instituto-data-cenario"))
    cards.append(metric_card("Cobertura historica", f"{polls['ano_eleicao'].nunique()} eleicoes", f"{int(polls['ano_eleicao'].min())}-{int(polls['ano_eleicao'].max())}"))
    cards.append(metric_card("Ultima pesquisa", latest_date.strftime("%d/%m/%Y"), "data de publicacao mais recente"))
if not ensemble_forecast.empty:
    principal = current_scenario(ensemble_forecast)
    if principal.empty:
        principal = ensemble_forecast.copy()
    principal["voto_valido_estimado"] = pd.to_numeric(principal["voto_valido_estimado"])
    principal["probabilidade_liderar"] = pd.to_numeric(principal["probabilidade_liderar"])
    leader = principal.sort_values("voto_valido_estimado", ascending=False).iloc[0]
    runner = principal.sort_values("voto_valido_estimado", ascending=False).iloc[1]
    margin = leader["voto_valido_estimado"] - runner["voto_valido_estimado"]
    cards.append(metric_card("Lideranca atual", leader["candidato"], f"{pct(leader['voto_valido_estimado'])} dos validos"))
    cards.append(metric_card("Margem estimada", pct(margin), f"liderar: {pct(100 * leader['probabilidade_liderar'])}"))
if cards:
    display(HTML('<div class="executive-grid">' + "".join(cards) + "</div>"))
```

## Resultado atual

O cenario operacional de agora e o oficial pos-TSE: pesquisas sem Pablo Marcal, publicadas a partir de 11/09/2026, somente com nomes de candidaturas aprovadas pelo TSE. Cenarios com Marcal ficam apenas como comparacao historica, porque o TSE indeferiu o registro da chapa.

```{python}
#| label: resultado-atual-tabela
if not ensemble_forecast.empty:
    view = current_scenario(ensemble_forecast)
    view = view.copy()
    view["voto_valido_estimado"] = pd.to_numeric(view["voto_valido_estimado"]).round(2)
    view["probabilidade_liderar"] = (pd.to_numeric(view["probabilidade_liderar"]) * 100).round(1)
    display(view[["cenario", "candidato", "voto_valido_estimado", "probabilidade_liderar", "modelo"]].sort_values("voto_valido_estimado", ascending=False).head(10))
```

```{python}
#| label: resultado-atual-grafico
#| fig-cap: "Nowcast principal: ensemble disciplinado"
if not ensemble_forecast.empty:
    plot = current_scenario(ensemble_forecast)
    plot = plot.copy()
    plot["voto_valido_estimado"] = pd.to_numeric(plot["voto_valido_estimado"])
    plot["probabilidade_liderar"] = pd.to_numeric(plot["probabilidade_liderar"])
    group = plot.sort_values("voto_valido_estimado", ascending=False).head(8).sort_values("voto_valido_estimado")
    fig, ax = plt.subplots(figsize=(9, 4.8))
    colors = [ORANGE if i >= len(group) - 2 else GREEN for i in range(len(group))]
    ax.barh(group["candidato"], group["voto_valido_estimado"], color=colors)
    ax.set_title("Lula e Flavio seguem destacados; Renan aparece melhor apos a coleta recente")
    ax.set_xlabel("Votos validos estimados (%)")
    ax.set_ylabel("")
    for i, row in enumerate(group.itertuples()):
        label = f"{row.voto_valido_estimado:.1f}%"
        if row.probabilidade_liderar > 0:
            label += f" | lidera {row.probabilidade_liderar:.0%}"
        ax.text(row.voto_valido_estimado + 0.4, i, label, va="center", fontsize=9)
    plt.tight_layout()
```

## Confianca da rodada

Esta secao resume se a previsao deve ser lida como sinal forte ou como fotografia ainda instavel. O placar combina apenas criterios auditaveis: recencia das pesquisas, quantidade de pesquisas no cenario operacional, auditoria de fontes, margem entre primeiro e segundo e erro historico do ensemble.

```{python}
#| label: confianca-rodada
score = 0
notes = []
cards = []

principal_polls = pd.DataFrame()
if not polls.empty:
    principal_polls = polls[polls["cenario_rotulo"].eq(CURRENT_SCENARIO_LABEL)].copy()
    unique_current = principal_polls.drop_duplicates(["instituto", "data_publicacao", "cenario"]).shape[0]
    last_date = pd.to_datetime(principal_polls["data_publicacao"], errors="coerce").max()
    if unique_current >= 12:
        score += 25
    elif unique_current >= 6:
        score += 18
    else:
        score += 8
    cards.append(metric_card("Pesquisas no cenario", unique_current, "instituto-data-cenario"))
    if pd.notna(last_date):
        days_old = (pd.Timestamp.today().normalize() - last_date.normalize()).days
        score += 20 if days_old <= 7 else 12 if days_old <= 21 else 5
        cards.append(metric_card("Recencia", f"{days_old} dias", f"ultima publicacao: {last_date.strftime('%d/%m/%Y')}"))

if not recent_audit.empty:
    audit_work = recent_audit.copy()
    audit_work["data_publicacao"] = pd.to_datetime(audit_work["data_publicacao"], errors="coerce")
    current_audit = audit_work[
        audit_work["cenario"].eq("agosto_setembro_2026_sem_marcal")
        & audit_work["data_publicacao"].ge(pd.Timestamp("2026-09-11"))
    ].copy()
    strong = current_audit["status_auditoria"].astype(str).str.contains("confirmad|primaria", case=False, na=False).sum()
    total = len(current_audit)
    ratio = strong / total if total else 0
    score += 20 if ratio >= 0.7 else 14 if ratio >= 0.45 else 8
    cards.append(metric_card("Auditoria recente", f"{strong}/{total}", "fontes fortes ou confirmadas"))

if not ensemble_forecast.empty:
    current = current_scenario(ensemble_forecast).copy()
    current["voto_valido_estimado"] = pd.to_numeric(current["voto_valido_estimado"], errors="coerce")
    ordered = current.sort_values("voto_valido_estimado", ascending=False)
    if len(ordered) >= 2:
        margin = float(ordered.iloc[0]["voto_valido_estimado"] - ordered.iloc[1]["voto_valido_estimado"])
        score += 15 if margin >= 8 else 10 if margin >= 4 else 5
        cards.append(metric_card("Margem lideranca", pct(margin), "quanto maior, mais estavel"))

if not backtest_vivo.empty:
    live = backtest_vivo.copy()
    live["mae"] = pd.to_numeric(live["mae"], errors="coerce")
    live_mae = live.groupby("modelo")["mae"].mean()
    ref_mae = float(live_mae.min())
    score += 20 if ref_mae <= 6 else 14 if ref_mae <= 8 else 8
    cards.append(metric_card("Erro vivo historico", f"{ref_mae:.2f} p.p.", "melhor modelo medio"))

score = min(score, 100)
display(HTML('<div class="executive-grid">' + "".join(cards) + "</div>"))
display(HTML(f"""
<div class="callout-box">
  <strong>Confianca operacional: {confidence_label(score).upper()} ({score}/100).</strong>
  A rodada esta bem alimentada e recente, mas a incerteza segue relevante porque a eleicao ainda esta longe
  e parte da base historica antiga ainda depende de auditoria complementar.
</div>
"""))
```

## Cenarios de 2026

```{python}
#| label: candidatos-2026-auditados
if not candidates_2026.empty:
    view = candidates_2026.copy()
    view["uso_modelo"] = view["usar_previsao_principal"].map({1: "entra no cenario operacional", 0: "fora do cenario operacional"})
    view = view.sort_values(["usar_previsao_principal", "candidato"], ascending=[False, True])
    display(view[["candidato", "partido", "vice", "status_tse", "uso_modelo", "observacao"]])
```

```{python}
#| label: cenarios-2026
scenarios_path = RAW / "cenarios.csv"
scenarios = read_csv(scenarios_path)
if not scenarios.empty:
    view = scenarios[scenarios["ano_eleicao"].eq(2026)].copy()
    view["uso"] = view["usar_previsao_principal"].map({1: "operacional", 0: "comparativo"}).fillna(view["usar_previsao_principal"])
    view["familia"] = view["rotulo"].map(
        lambda x: "principal atual" if x == CURRENT_SCENARIO_LABEL
        else "com Marcal" if "Marcal" in str(x) or "Marçal" in str(x)
        else "comparativo antigo" if "Lula vs Flavio" in str(x)
        else "alternativo"
    )
    display(view[["rotulo", "familia", "uso", "prioridade", "observacao"]].sort_values(["uso", "prioridade", "rotulo"]))
```

```{python}
#| label: cenarios-2026-leitura
display(HTML("""
<div class="callout-box">
  <strong>Leitura:</strong> o relatorio separa cenario operacional e cenarios alternativos.
  Isso evita misturar candidatos sem registro aprovado ou listas antigas com a previsao principal.
</div>
"""))
```

## O que mudou com as pesquisas novas

A coleta recente nao muda so o tamanho da base. Ela troca uma fotografia mais estreita da disputa por um cenario com Renan Santos e candidatos menores, que antes ficavam ausentes ou pouco visiveis na coleta automatica.

```{python}
#| label: impacto-coleta-recente
if not ensemble_forecast.empty:
    view = ensemble_forecast.copy()
    view["voto_valido_estimado"] = pd.to_numeric(view["voto_valido_estimado"])
    view["probabilidade_liderar"] = pd.to_numeric(view["probabilidade_liderar"])
    old = view[view["cenario"].eq("Primeiro turno 2026 - Lula vs Flavio")]
    new = current_scenario(view)
    rows = []
    for candidate in sorted(set(old["candidato"]) | set(new["candidato"])):
        old_value = old.loc[old["candidato"].eq(candidate), "voto_valido_estimado"]
        new_value = new.loc[new["candidato"].eq(candidate), "voto_valido_estimado"]
        rows.append({
            "candidato": candidate,
            "cenario_anterior": round(float(old_value.iloc[0]), 2) if not old_value.empty else None,
            "cenario_recente": round(float(new_value.iloc[0]), 2) if not new_value.empty else None,
            "mudanca": round(float(new_value.iloc[0] - old_value.iloc[0]), 2) if not old_value.empty and not new_value.empty else None,
        })
    impact = pd.DataFrame(rows)
    display(impact.sort_values("cenario_recente", ascending=False, na_position="last").head(12))
```

```{python}
#| label: impacto-coleta-recente-grafico
#| fig-cap: "Impacto da coleta recente no cenario principal"
if not ensemble_forecast.empty:
    view = ensemble_forecast.copy()
    view["voto_valido_estimado"] = pd.to_numeric(view["voto_valido_estimado"])
    old = view[view["cenario"].eq("Primeiro turno 2026 - Lula vs Flavio")]
    new = current_scenario(view)
    focus = ["Lula", "Flávio Bolsonaro", "Augusto Cury", "Renan Santos", "Ronaldo Caiado", "Romeu Zema"]
    rows = []
    for candidate in focus:
        old_value = old.loc[old["candidato"].eq(candidate), "voto_valido_estimado"]
        new_value = new.loc[new["candidato"].eq(candidate), "voto_valido_estimado"]
        rows.append({
            "candidato": candidate,
            "anterior": float(old_value.iloc[0]) if not old_value.empty else 0,
            "recente": float(new_value.iloc[0]) if not new_value.empty else 0,
        })
    plot = pd.DataFrame(rows).sort_values("recente")
    y = range(len(plot))
    fig, ax = plt.subplots(figsize=(9, 4.4))
    ax.scatter(plot["anterior"], y, color=GRAY, s=55, label="cenario anterior")
    ax.scatter(plot["recente"], y, color=ORANGE, s=70, label="oficial pos-TSE")
    for i, row in enumerate(plot.itertuples()):
        ax.plot([row.anterior, row.recente], [i, i], color=GRAY, alpha=0.45)
        ax.text(row.recente + 0.35, i, f"{row.recente:.1f}", va="center", fontsize=9)
    ax.set_yticks(list(y))
    ax.set_yticklabels(plot["candidato"])
    ax.set_xlabel("Votos validos estimados (%)")
    ax.set_title("Renan passa a aparecer explicitamente; a margem Lula-Flavio fica menor")
    ax.legend()
    plt.tight_layout()
```

```{python}
#| label: impacto-coleta-recente-resumo
recent_raw = read_csv(RAW / "pesquisas_2026_agosto_setembro_manual.csv")
if not recent_raw.empty:
    principal = recent_raw[recent_raw["cenario"].eq("agosto_setembro_2026_sem_marcal")]
    alternativo = recent_raw[recent_raw["cenario"].eq("agosto_setembro_2026_com_marcal")]
    principal_unique = principal.drop_duplicates(["instituto", "data_publicacao", "cenario"]).shape[0]
    alternativo_unique = alternativo.drop_duplicates(["instituto", "data_publicacao", "cenario"]).shape[0]
    display(HTML(f"""
    <div class="callout-box">
      <strong>Coleta adicionada:</strong> {len(recent_raw)} linhas candidato-pesquisa manuais.
      Isso corresponde a {principal_unique + alternativo_unique} pesquisas/cenarios unicos recentes:
      {principal_unique} no cenario sem Marcal e {alternativo_unique} no cenario alternativo com Marcal.
    </div>
    """))
```

## Historico de previsoes

Cada execucao do nowcast pode ser registrada como snapshot. Isso permite acompanhar como a previsao muda conforme novas pesquisas entram ou a base historica e revisada.

```{python}
#| label: historico-previsoes-resumo
if not forecast_history.empty:
    view = forecast_history.copy()
    snapshots = view[["data_registro", "data_previsao"]].drop_duplicates().sort_values("data_registro")
    display(snapshots)
```

```{python}
#| label: historico-previsoes-grafico
#| fig-cap: "Evolucao registrada do nowcast por snapshot"
if not forecast_history.empty:
    hist = forecast_history.copy()
    hist = current_scenario(hist)
    hist["data_registro"] = pd.to_datetime(hist["data_registro"], errors="coerce")
    hist["voto_valido_estimado"] = pd.to_numeric(hist["voto_valido_estimado"], errors="coerce")
    focus = hist.groupby("candidato")["voto_valido_estimado"].mean().sort_values(ascending=False).head(6).index
    hist = hist[hist["candidato"].isin(focus)]
    fig, ax = plt.subplots(figsize=(9, 4.4))
    for candidate, group in hist.groupby("candidato"):
        series = group.groupby("data_registro")["voto_valido_estimado"].mean().sort_index()
        ax.plot(series.index, series.values, marker="o", label=candidate)
    ax.set_title("Historico comeca agora; proximas pesquisas criam a serie viva")
    ax.set_xlabel("Data de registro da rodada")
    ax.set_ylabel("Votos validos estimados (%)")
    ax.legend()
    plt.tight_layout()
```

## Fontes e referencias

As fontes entram em tres camadas:

1. **Resultado oficial:** Dados Abertos do TSE, para votos validos do primeiro turno.
2. **Pesquisas registradas:** PesqEle/TSE e fontes primarias dos institutos, para pesquisas atuais e historicas.
3. **Fundamentos objetivos:** variaveis politicas e economicas usadas na literatura de previsao eleitoral.

Referencias uteis para desenho do modelo:

- FiveThirtyEight/538: uso de medias de pesquisas, ajuste por qualidade do instituto, incerteza e simulacoes.
- The Economist: combinacao de pesquisas, fundamentos, estrutura hierarquica e simulacoes probabilisticas.
- Alan Abramowitz, *Time-for-Change*: aprovacao presidencial, crescimento economico e tempo do partido no poder.
- Douglas Hibbs, *Bread and Peace*: renda real e custo politico de guerras como explicadores do voto governista nos EUA.
- Cole Nussbaumer Knaflic, *Storytelling with Data*: foco em mensagem, remocao de ruido visual e destaque explicito do que importa.

## Variaveis do primeiro modelo

O baseline usa variaveis que saem diretamente das pesquisas ou de calendario:

- voto total do candidato na pesquisa;
- brancos, nulos e indecisos;
- voto valido estimado;
- instituto;
- tamanho da amostra;
- data de campo;
- data de publicacao;
- dias entre fim do campo e primeiro turno;
- dias entre publicacao e primeiro turno;
- ordem da pesquisa dentro do instituto e cenario;
- ordem geral da pesquisa na campanha.
- ajuste por vies historico do instituto, quando ha dados suficientes;
- efeito casa por instituto e candidato, quando ha dados suficientes;
- peso por qualidade historica do instituto.

```{python}
#| label: cobertura-eleicoes
#| fig-cap: "Eleicoes cadastradas no projeto"
if elections.empty:
    fig, ax = plt.subplots()
    ax.text(0.5, 0.5, "Nenhuma eleicao cadastrada ainda", ha="center", va="center", fontsize=14)
    ax.axis("off")
else:
    plot = elections.assign(ano_eleicao=elections["ano_eleicao"].astype(str))
    fig, ax = plt.subplots(figsize=(9, 2.8))
    ax.barh(plot["ano_eleicao"], [1] * len(plot), color=BLUE)
    ax.set_xlabel("Cadastro da eleicao")
    ax.set_ylabel("Ano")
    ax.set_xticks([])
    ax.set_title("Calendario historico para calcular distancia ate a eleicao")
    for y, obs in enumerate(plot["observacao"].fillna("")):
        ax.text(1.02, y, obs[:70], va="center", fontsize=8)
    ax.set_xlim(0, 2.4)
    plt.tight_layout()
```

```{python}
#| label: cobertura-por-ano
#| fig-cap: "Pesquisas/cenarios unicos por eleicao"
if not polls.empty:
    counts = (
        polls.groupby("ano_eleicao")
        .agg(
            linhas=("candidato", "size"),
            pesquisas_cenarios=("data_publicacao", lambda s: 0),
        )
        .reset_index()
    )
    uniques = polls.drop_duplicates(["ano_eleicao", "instituto", "data_publicacao", "cenario"]).groupby("ano_eleicao").size()
    counts["pesquisas_cenarios"] = counts["ano_eleicao"].map(uniques).fillna(0).astype(int)
    fig, ax = plt.subplots(figsize=(9, 3.8))
    colors = [ORANGE if year == 2026 else BLUE for year in counts["ano_eleicao"]]
    ax.bar(counts["ano_eleicao"].astype(str), counts["pesquisas_cenarios"], color=colors)
    ax.set_title("Pesquisas unicas crescem nas eleicoes recentes; anos antigos dependem de acervo")
    ax.set_xlabel("Ano da eleicao")
    ax.set_ylabel("Pesquisas/cenarios unicos")
    for i, row in enumerate(counts.itertuples()):
        ax.text(
            i,
            row.pesquisas_cenarios + counts["pesquisas_cenarios"].max() * 0.02,
            f"{row.pesquisas_cenarios}\n({row.linhas} linhas)",
            ha="center",
            fontsize=8,
        )
    plt.tight_layout()
```

## Cobertura real da base

Uma linha do arquivo processado e uma combinacao candidato-pesquisa. Uma pesquisa com oito candidatos vira oito linhas. Para avaliar a cobertura real, a unidade correta e a combinacao `ano + instituto + data + cenario`.

```{python}
#| label: cobertura-real-tabela
if not polls.empty:
    base = polls.copy()
    base["amostra_imputada"] = pd.to_numeric(base.get("amostra_imputada", 0), errors="coerce").fillna(0)
    unique = base.drop_duplicates(["ano_eleicao", "instituto", "data_publicacao", "cenario"]).copy()
    quality_terms = "primaria|confirmad|oficial|poderdata"
    unique["fonte_forte"] = unique["qualidade_fonte"].astype(str).str.contains(quality_terms, case=False, na=False)
    summary = (
        base.groupby("ano_eleicao")
        .agg(
            linhas_candidato_pesquisa=("candidato", "size"),
            candidatos=("candidato", "nunique"),
            institutos=("instituto", "nunique"),
        )
        .join(unique.groupby("ano_eleicao").size().rename("pesquisas_cenarios"))
        .join(unique.groupby("ano_eleicao")["fonte_forte"].mean().mul(100).rename("fonte_forte_pct"))
        .join(unique.groupby("ano_eleicao")["amostra_imputada"].mean().mul(100).rename("amostra_imputada_pct"))
        .reset_index()
    )
    for col in ["fonte_forte_pct", "amostra_imputada_pct"]:
        summary[col] = summary[col].round(1)
    display(summary[[
        "ano_eleicao",
        "pesquisas_cenarios",
        "linhas_candidato_pesquisa",
        "institutos",
        "candidatos",
        "fonte_forte_pct",
        "amostra_imputada_pct",
    ]])
```

```{python}
#| label: cobertura-real-grafico
#| fig-cap: "Linhas candidato-pesquisa versus pesquisas/cenarios unicos"
if not polls.empty:
    unique = polls.drop_duplicates(["ano_eleicao", "instituto", "data_publicacao", "cenario"]).groupby("ano_eleicao").size()
    lines = polls.groupby("ano_eleicao").size()
    summary = pd.DataFrame({"linhas": lines, "pesquisas_cenarios": unique}).reset_index()
    x = range(len(summary))
    fig, ax1 = plt.subplots(figsize=(9, 4.2))
    ax2 = ax1.twinx()
    ax1.bar([i - 0.18 for i in x], summary["pesquisas_cenarios"], width=0.36, color=BLUE, label="pesquisas/cenarios")
    ax2.bar([i + 0.18 for i in x], summary["linhas"], width=0.36, color=GRAY, alpha=0.55, label="linhas candidato-pesquisa")
    ax1.set_xticks(list(x))
    ax1.set_xticklabels(summary["ano_eleicao"].astype(str))
    ax1.set_ylabel("Pesquisas/cenarios unicos")
    ax2.set_ylabel("Linhas candidato-pesquisa")
    ax1.set_title("Quantidade real de pesquisas e tamanho tabular sao coisas diferentes")
    handles1, labels1 = ax1.get_legend_handles_labels()
    handles2, labels2 = ax2.get_legend_handles_labels()
    ax1.legend(handles1 + handles2, labels1 + labels2, loc="upper left")
    plt.tight_layout()
```

## Qualidade da base historica

Esta leitura separa volume de confiabilidade. Uma eleicao pode ter muitas linhas e ainda depender de amostras imputadas, series consolidadas ou fontes secundarias. Isso importa porque o backtest deve punir menos um modelo quando a propria base historica daquele ano e fraca.

```{python}
#| label: qualidade-fonte-por-ano
if not polls.empty:
    base = polls.copy()
    unique = base.drop_duplicates(["ano_eleicao", "instituto", "data_publicacao", "cenario"]).copy()
    unique["amostra_imputada"] = pd.to_numeric(unique.get("amostra_imputada", 0), errors="coerce").fillna(0)

    def fonte_categoria(value):
        text = str(value).lower()
        if "primaria" in text or "confirmad" in text or "oficial" in text:
            return "fonte forte"
        if "secundaria" in text or "folha" in text or "gazeta" in text:
            return "fonte secundaria auditada"
        if "wikipedia" in text or "pesqele" in text or "normalizada" in text:
            return "base agregada"
        return "outra"

    unique["categoria_fonte"] = unique["qualidade_fonte"].map(fonte_categoria)
    summary = (
        unique.groupby(["ano_eleicao", "categoria_fonte"], as_index=False)
        .size()
        .rename(columns={"size": "pesquisas_cenarios"})
    )
    total = summary.groupby("ano_eleicao")["pesquisas_cenarios"].transform("sum")
    summary["pct"] = (summary["pesquisas_cenarios"] / total * 100).round(1)
    display(summary.sort_values(["ano_eleicao", "pesquisas_cenarios"], ascending=[True, False]))
```

```{python}
#| label: qualidade-fonte-grafico
#| fig-cap: "Composicao das fontes por ano"
if not polls.empty:
    base = polls.copy()
    unique = base.drop_duplicates(["ano_eleicao", "instituto", "data_publicacao", "cenario"]).copy()
    unique["categoria_fonte"] = unique["qualidade_fonte"].map(fonte_categoria)
    pivot = unique.pivot_table(index="ano_eleicao", columns="categoria_fonte", values="cenario", aggfunc="count", fill_value=0)
    order = [c for c in ["fonte forte", "fonte secundaria auditada", "base agregada", "outra"] if c in pivot.columns]
    pivot = pivot[order]
    colors = {
        "fonte forte": GREEN,
        "fonte secundaria auditada": BLUE,
        "base agregada": ORANGE,
        "outra": GRAY,
    }
    fig, ax = plt.subplots(figsize=(9, 4.3))
    bottom = None
    for col in pivot.columns:
        ax.bar(pivot.index.astype(str), pivot[col], bottom=bottom, label=col, color=colors.get(col, GRAY))
        bottom = pivot[col] if bottom is None else bottom + pivot[col]
    ax.set_title("Nem toda cobertura tem a mesma confiabilidade")
    ax.set_xlabel("Ano da eleicao")
    ax.set_ylabel("Pesquisas/cenarios unicos")
    ax.legend(title="Categoria da fonte", bbox_to_anchor=(1.02, 1), loc="upper left")
    plt.tight_layout()
```

```{python}
#| label: risco-base-por-ano
if not polls.empty:
    base = polls.copy()
    unique = base.drop_duplicates(["ano_eleicao", "instituto", "data_publicacao", "cenario"]).copy()
    unique["amostra_imputada"] = pd.to_numeric(unique.get("amostra_imputada", 0), errors="coerce").fillna(0)
    unique["categoria_fonte"] = unique["qualidade_fonte"].map(fonte_categoria)
    risk = (
        unique.groupby("ano_eleicao")
        .agg(
            pesquisas_cenarios=("cenario", "size"),
            institutos=("instituto", "nunique"),
            fonte_forte_pct=("categoria_fonte", lambda s: (s.eq("fonte forte").mean() * 100)),
            base_agregada_pct=("categoria_fonte", lambda s: (s.eq("base agregada").mean() * 100)),
            amostra_imputada_pct=("amostra_imputada", lambda s: (s.mean() * 100)),
        )
        .reset_index()
    )
    def risco(row):
        score = 0
        if row["pesquisas_cenarios"] < 10:
            score += 1
        if row["institutos"] < 3:
            score += 1
        if row["amostra_imputada_pct"] > 50:
            score += 1
        if row["base_agregada_pct"] > 50:
            score += 1
        return "alto" if score >= 3 else "medio" if score >= 1 else "baixo"
    risk["risco_leitura"] = risk.apply(risco, axis=1)
    for col in ["fonte_forte_pct", "base_agregada_pct", "amostra_imputada_pct"]:
        risk[col] = risk[col].round(1)
    display(risk.sort_values("ano_eleicao"))
```

## Variaveis objetivas para incluir logo

A literatura e modelos profissionais costumam combinar pesquisas com fundamentos. Para o Brasil, algumas variaveis sao objetivas o suficiente para entrar cedo, desde que a fonte seja consistente:

- **incumbencia:** se o candidato e presidente tentando reeleicao;
- **sucessor governista:** candidato apoiado pelo presidente quando o presidente nao disputa;
- **tempo do grupo no poder:** quantos mandatos consecutivos o campo governista esta no governo;
- **aprovacao do governo:** percentual de otimo/bom, regular, ruim/pessimo ou aprovacao/desaprovacao;
- **economia:** inflacao, desemprego, crescimento do PIB, renda real e indicadores de percepcao economica;
- **rejeicao:** percentual que declara nao votar no candidato;
- **conhecimento do candidato:** quando disponivel em pesquisas;
- **metodo da pesquisa:** presencial, telefone, online ou misto;
- **qualidade historica do instituto:** erro medio passado e frequencia de pesquisas.

Essas variaveis devem ser comparadas por backtest. Se nao melhorarem a previsao fora da amostra, ficam como explicacao, nao como motor do modelo final.

```{python}
#| label: fundamentos-economicos
if not fundamentals.empty:
    view = fundamentals.copy()
    for col in ["pib_crescimento", "inflacao", "desemprego"]:
        view[col] = pd.to_numeric(view[col], errors="coerce").round(2)
    display(view[["ano_eleicao", "fundamentos_ano_referencia", "pib_crescimento", "inflacao", "desemprego", "fundamentos_defasagem_max"]])
```

## Organizacao dos dados

```{python}
#| label: schema-visual
#| fig-cap: "Fluxo dos dados ate a previsao"
fig, ax = plt.subplots(figsize=(10, 4))
ax.axis("off")
steps = [
    ("Pesquisas", "CSV bruto\\npor candidato"),
    ("Resultados", "TSE\\nvotos validos"),
    ("Preparacao", "validos, dias,\\nordem, erro"),
    ("Modelos", "baseline, inferencia,\\nML, IA"),
    ("Saida", "previsao datada\\n+ incerteza"),
]
xs = [0.08, 0.29, 0.50, 0.71, 0.92]
for i, ((title, body), x) in enumerate(zip(steps, xs)):
    ax.scatter([x], [0.6], s=1700, color="#4C78A8")
    ax.text(x, 0.64, title, ha="center", va="center", color="white", weight="bold")
    ax.text(x, 0.33, body, ha="center", va="center", fontsize=9)
    if i < len(xs) - 1:
        ax.annotate("", xy=(xs[i+1]-0.06, 0.6), xytext=(x+0.06, 0.6), arrowprops={"arrowstyle": "->", "lw": 1.5})
ax.set_title("Do dado bruto ao nowcast eleitoral")
plt.tight_layout()
```

## Auditoria da base

O gargalo atual nao e algoritmo: e confiabilidade das series antigas. A base ja separa fonte, instituto, amostra imputada e cenario para permitir auditoria incremental. A busca mais recente adicionou Datafolha e Ibope para 1994 e 1998, mas ainda vale priorizar relatorios primarios completos e novas tabelas extraiveis desses anos.

```{python}
#| label: auditoria-recente-2026-resumo
if not recent_audit.empty:
    summary = recent_audit.groupby("status_auditoria", as_index=False).size().rename(columns={"size": "pesquisas_cenarios"})
    display(summary.sort_values("pesquisas_cenarios", ascending=False))
```

```{python}
#| label: auditoria-operacional-2026
if not recent_audit.empty:
    view = recent_audit.copy()
    view["data_publicacao"] = pd.to_datetime(view["data_publicacao"], errors="coerce")
    view["recorte_tse"] = view["data_publicacao"].ge(pd.Timestamp("2026-09-11")).map({True: "pos-TSE", False: "pre-TSE"})
    view["cenario_status"] = view["cenario"].map(
        lambda x: "obsoleto: inclui Marcal" if "com_marcal" in str(x)
        else "operacional" if "sem_marcal" in str(x)
        else "outro"
    )
    view["entra_operacional"] = (
        view["recorte_tse"].eq("pos-TSE") & view["cenario_status"].eq("operacional")
    ).map({True: "sim", False: "nao"})
    summary = (
        view.groupby(["entra_operacional", "recorte_tse", "cenario_status", "status_auditoria"], as_index=False)
        .size()
        .rename(columns={"size": "pesquisas_cenarios"})
    )
    display(summary.sort_values(["entra_operacional", "recorte_tse", "cenario_status"], ascending=[False, True, True]))
```

```{python}
#| label: auditoria-recente-2026-detalhe
if not recent_audit.empty:
    view = recent_audit.copy()
    view["data_publicacao_dt"] = pd.to_datetime(view["data_publicacao"], errors="coerce")
    view["entra_operacional"] = (
        view["cenario"].eq("agosto_setembro_2026_sem_marcal")
        & view["data_publicacao_dt"].ge(pd.Timestamp("2026-09-11"))
    ).map({True: "sim", False: "nao"})
    display(view[["entra_operacional", "instituto", "data_publicacao", "cenario", "registro_tse", "status_auditoria", "observacao_auditoria"]].sort_values(["entra_operacional", "data_publicacao", "instituto"], ascending=[False, False, True]))
```

```{python}
#| label: auditoria-fontes
if not source_audit.empty:
    view = source_audit.copy()
    display(view[["ano_eleicao", "fonte", "linhas", "pesquisas_unicas", "institutos", "amostra_imputada", "valor_fora_0_100", "primeira_publicacao", "ultima_publicacao"]])
```

## Analise exploratoria planejada

Assim que a base de pesquisas estiver preenchida, os primeiros graficos devem responder:

- quais institutos aparecem por ano;
- quantas pesquisas existem por semana de campanha;
- como cada candidato evolui em votos validos estimados;
- como o erro historico muda conforme a eleicao se aproxima;
- se algum instituto tem viés medio recorrente;
- se pesquisas online, presenciais e telefonicas se comportam diferente;
- se rejeicao, aprovacao do governo e economia ajudam alem das pesquisas.

```{python}
#| label: pesquisas-por-instituto
#| fig-cap: "Cobertura atual de pesquisas por instituto"
data = polls if not polls.empty else raw_polls
if data.empty or "instituto" not in data:
    fig, ax = plt.subplots()
    ax.text(0.5, 0.5, "Base de pesquisas ainda vazia", ha="center", va="center", fontsize=14)
    ax.text(0.5, 0.40, "Ao adicionar linhas em data/raw/pesquisas.csv, este grafico passa a mostrar cobertura por instituto.", ha="center", va="center", fontsize=9)
    ax.axis("off")
else:
    counts = data.drop_duplicates(["ano_eleicao", "instituto", "data_publicacao", "cenario"]).groupby("instituto").size().sort_values().tail(25)
    fig, ax = plt.subplots()
    counts.plot(kind="barh", ax=ax, color=BLUE)
    ax.set_xlabel("Pesquisas")
    ax.set_ylabel("Instituto")
    ax.set_title("Institutos com maior cobertura na base")
    plt.tight_layout()
```

```{python}
#| label: previsao-atual
#| fig-cap: "Previsao atual por cenario principal de 2026"
if forecasts.empty:
    fig, ax = plt.subplots()
    ax.text(0.5, 0.5, "Sem previsao atual", ha="center", va="center", fontsize=14)
    ax.axis("off")
else:
    tmp = forecasts.copy()
    tmp["voto_valido_estimado"] = pd.to_numeric(tmp["voto_valido_estimado"])
    tmp["intervalo_baixo"] = pd.to_numeric(tmp["intervalo_baixo"])
    tmp["intervalo_alto"] = pd.to_numeric(tmp["intervalo_alto"])
    tmp["probabilidade_liderar"] = pd.to_numeric(tmp["probabilidade_liderar"])
    tmp["modelo"] = tmp.get("modelo", "baseline_pesquisa")
    tmp = tmp.sort_values(["cenario", "modelo", "voto_valido_estimado"], ascending=[True, True, False])
    panels = tmp[["cenario", "modelo"]].drop_duplicates().itertuples(index=False)
    panels = list(panels)
    fig, axes = plt.subplots(len(panels), 1, figsize=(9, max(3, 2.6 * len(panels))), squeeze=False)
    for ax, panel in zip(axes.flat, panels):
        group = tmp[(tmp["cenario"] == panel.cenario) & (tmp["modelo"] == panel.modelo)].head(6).sort_values("voto_valido_estimado")
        lower = group["voto_valido_estimado"] - group["intervalo_baixo"]
        upper = group["intervalo_alto"] - group["voto_valido_estimado"]
        colors = [ORANGE if i >= len(group) - 2 else BLUE for i in range(len(group))]
        ax.barh(group["candidato"], group["voto_valido_estimado"], xerr=[lower, upper], color=colors, ecolor=DARK, capsize=3)
        ax.set_title(f"{panel.cenario} | {panel.modelo}")
        ax.set_xlabel("Votos validos estimados (%)")
        for i, row in enumerate(group.itertuples()):
            ax.text(row.intervalo_alto + 0.5, i, f"{row.voto_valido_estimado:.1f} | lidera {row.probabilidade_liderar:.0%}", va="center", fontsize=9)
    plt.tight_layout()
```

```{python}
#| label: evolucao-candidatos
#| fig-cap: "Evolucao de votos validos estimados"
if polls.empty or "voto_valido_estimado" not in polls:
    fig, ax = plt.subplots()
    ax.text(0.5, 0.5, "Sem serie temporal ainda", ha="center", va="center", fontsize=14)
    ax.text(0.5, 0.40, "Quando houver pesquisas processadas, as linhas mostram a trajetoria por candidato.", ha="center", va="center", fontsize=9)
    ax.axis("off")
else:
    tmp = polls.copy()
    tmp["data_publicacao"] = pd.to_datetime(tmp["data_publicacao"])
    tmp["voto_valido_estimado"] = pd.to_numeric(tmp["voto_valido_estimado"])
    latest_year = tmp["ano_eleicao"].max()
    tmp = tmp[tmp["ano_eleicao"] == latest_year]
    fig, ax = plt.subplots()
    top_candidates = tmp.groupby("candidato")["voto_valido_estimado"].mean().sort_values(ascending=False).head(8).index
    tmp = tmp[tmp["candidato"].isin(top_candidates)]
    for candidate, group in tmp.groupby("candidato"):
        series = group.groupby("data_publicacao")["voto_valido_estimado"].mean().sort_index()
        ax.plot(series.index, series.values, marker="o", label=candidate)
    ax.set_ylabel("Votos validos estimados (%)")
    ax.set_xlabel("Data de publicacao")
    ax.set_title(f"Evolucao das pesquisas em {latest_year}")
    ax.legend()
    plt.tight_layout()
```

```{python}
#| label: previsao-ensemble
#| fig-cap: "Previsao final do ensemble disciplinado"
if ensemble_forecast.empty:
    fig, ax = plt.subplots()
    ax.text(0.5, 0.5, "Sem previsao ensemble ainda", ha="center", va="center", fontsize=14)
    ax.axis("off")
else:
    tmp = ensemble_forecast.copy()
    tmp["voto_valido_estimado"] = pd.to_numeric(tmp["voto_valido_estimado"])
    tmp["probabilidade_liderar"] = pd.to_numeric(tmp["probabilidade_liderar"])
    scenarios = list(tmp["cenario"].drop_duplicates())
    fig, axes = plt.subplots(len(scenarios), 1, figsize=(9, max(3, 2.7 * len(scenarios))), squeeze=False)
    for ax, scenario in zip(axes.flat, scenarios):
        group = tmp[tmp["cenario"] == scenario].head(6).sort_values("voto_valido_estimado")
        colors = [ORANGE if i >= len(group) - 2 else GREEN for i in range(len(group))]
        ax.barh(group["candidato"], group["voto_valido_estimado"], color=colors)
        ax.set_title(scenario)
        ax.set_xlabel("Votos validos estimados (%)")
        for i, row in enumerate(group.itertuples()):
            ax.text(row.voto_valido_estimado + 0.5, i, f"{row.voto_valido_estimado:.1f} | lidera {row.probabilidade_liderar:.0%}", va="center", fontsize=9)
    plt.tight_layout()
```

## Modelos

Uma revisao mais completa da literatura esta em `docs/revisao-literatura-metodos.md`. A leitura principal e simples: pesquisas dominam perto da eleicao; fundamentos ajudam longe da eleicao; modelos Bayesianos dinamicos sao a evolucao estatistica mais natural; ML, redes neurais e ensembles entram como desafiantes do baseline, nao como substitutos automaticos.

```{python}
#| label: modelos-operacionais
rows = [
    ["ensemble_disciplinado", "Producao", "Combina baseline e Bayesiano dinamico com pesos por backtest vivo."],
    ["baseline_pesquisa", "Referencia", "Media ponderada por pesquisas; e o rival minimo a ser vencido."],
    ["bayesiano_dinamico_kalman", "Producao assistida", "Estado latente temporal; melhora incerteza e leitura da corrida."],
    ["random_forest_07_600 / gradient_boosting", "Experimental", "ML tabular competitivo, mas ainda instavel entre eleicoes."],
    ["mlp_pequena", "Experimental", "Deep learning pequeno; mantido como teste, nao como decisor."],
    ["voting_ia / stacking_ia", "Experimental", "IA/ensemble supervisionado; so entra se vencer fora da amostra."],
]
display(pd.DataFrame(rows, columns=["modelo", "status", "papel no relatorio"]))
```

```{python}
#| label: comparacao-curta-modelos
if not backtest.empty:
    view = backtest.copy()
    view["mae"] = pd.to_numeric(view["mae"])
    resumo = view.groupby("modelo", as_index=False)["mae"].mean().sort_values("mae")
    resumo["mae"] = resumo["mae"].round(2)
    display(resumo.head(8))
```

### Baseline

Media ponderada por recencia e amostra. Tambem ha versoes com qualidade historica do instituto e efeito casa por instituto-candidato. O baseline puro continua sendo o primeiro rival a ser batido.

### Inferencia

Modelo para explicar erro historico:

```text
erro ~ dias_ate_eleicao + incumbente + sucessor_governo + aprovacao_governo + economia + metodo + instituto
```

### Machine learning

Modelos candidatos:

- regressao regularizada;
- random forest;
- gradient boosting;
- ensemble simples.

### IA e deep learning

So entram depois de backtest suficiente. A base presidencial brasileira e pequena, entao uma rede neural deve ser pequena e tratada como experimento, nao como previsao principal.

```{python}
#| label: literatura-modelos-tabela
modelos_literatura = pd.DataFrame([
    ["Media de pesquisas", "Agregacao de pesquisas recentes", "baseline_pesquisa", "Referencia principal"],
    ["Inferencia", "Regressao explicavel para erro e variaveis politicas", "inferencial_ols", "Diagnostico"],
    ["ML tabular", "Regularizacao, margem, arvores e boosting", "ridge, elastic_net, huber, svr_rbf, random_forest com ntree/mtry", "Desafiantes"],
    ["Deep learning", "MLP tabular pequena/media/profunda", "mlp_pequena, mlp_media, mlp_profunda", "Experimental"],
    ["Efeito casa", "Correcao por vies historico de instituto-candidato", "baseline_efeito_casa", "Melhor baseline atual"],
    ["IA / ensemble", "Combinacao disciplinada de modelos", "voting_ia, stacking_ia, ensemble_pesos", "Auxiliar"],
    ["Bayesiano dinamico", "Estado latente da corrida no tempo", "bayesiano_dinamico_kalman", "Nowcast robusto em teste"],
], columns=["familia", "referencia conceitual", "modelo no projeto", "papel"])
display(modelos_literatura)
```

### Bayesiano dinamico

O `bayesiano_dinamico_kalman` trata a corrida como estado latente diario. Cada pesquisa e uma observacao ruidosa dessa trajetoria; o filtro de Kalman estima o nivel atual e projeta ate o primeiro turno com intervalo de incerteza. E mais honesto que a tendencia linear simples porque separa ruido observado de movimento temporal estimado.

No backtest vivo inicial, ele quase empata com o `baseline_temporal` e supera os baselines nao temporais. Por isso, entra no ensemble disciplinado com peso condicionado ao desempenho na previsao viva.

```{python}
#| label: previsao-bayesiana-dinamica
#| fig-cap: "Previsao 2026 pelo estado latente dinamico"
if bayesian_forecast.empty:
    fig, ax = plt.subplots()
    ax.text(0.5, 0.5, "Sem previsao bayesiana dinamica ainda", ha="center", va="center", fontsize=14)
    ax.axis("off")
else:
    tmp = bayesian_forecast.copy()
    for col in ["voto_valido_estimado", "intervalo_baixo", "intervalo_alto", "probabilidade_liderar"]:
        tmp[col] = pd.to_numeric(tmp[col])
    scenarios = list(tmp["cenario"].drop_duplicates())
    fig, axes = plt.subplots(len(scenarios), 1, figsize=(9, max(3, 2.8 * len(scenarios))), squeeze=False)
    for ax, scenario in zip(axes.flat, scenarios):
        group = tmp[tmp["cenario"] == scenario].head(6).sort_values("voto_valido_estimado")
        lower = group["voto_valido_estimado"] - group["intervalo_baixo"]
        upper = group["intervalo_alto"] - group["voto_valido_estimado"]
        ax.barh(group["candidato"], group["voto_valido_estimado"], xerr=[lower, upper], color="#6B6ECF", ecolor="#333333", capsize=3)
        ax.set_title(scenario)
        ax.set_xlabel("Votos validos estimados (%)")
        for i, row in enumerate(group.itertuples()):
            ax.text(row.intervalo_alto + 0.5, i, f"{row.voto_valido_estimado:.1f} | lidera {row.probabilidade_liderar:.0%}", va="center", fontsize=9)
    plt.tight_layout()
```

```{python}
#| label: tabela-bayesiana-dinamica
if not bayesian_forecast.empty:
    view = bayesian_forecast.copy()
    for col in ["voto_valido_estimado", "intervalo_baixo", "intervalo_alto", "probabilidade_liderar", "probabilidade_ir_ao_segundo_turno"]:
        view[col] = pd.to_numeric(view[col]).round(4)
    display(view[["cenario", "candidato", "voto_valido_estimado", "intervalo_baixo", "intervalo_alto", "probabilidade_liderar", "probabilidade_ir_ao_segundo_turno", "metodo_estado_latente"]])
```

```{python}
#| label: qualidade-institutos
if not quality.empty:
    view = quality.copy()
    view["mae"] = pd.to_numeric(view["mae"]).round(2)
    view["vies_medio"] = pd.to_numeric(view["vies_medio"]).round(2)
    display(view[["instituto", "linhas", "vies_medio", "mae"]].sort_values(["mae", "linhas"], ascending=[True, False]).head(20))
```

```{python}
#| label: efeito-casa
if not house_effect.empty:
    view = house_effect.copy()
    view["mae"] = pd.to_numeric(view["mae"]).round(2)
    view["vies_medio"] = pd.to_numeric(view["vies_medio"]).round(2)
    display(view[["instituto", "candidato", "linhas", "vies_medio", "mae"]].sort_values(["mae", "linhas"], ascending=[True, False]).head(20))
```

## Criterio de comparacao

Cada modelo deve ser avaliado por backtest historico:

- erro medio absoluto por candidato;
- acerto dos dois candidatos no segundo turno;
- erro do primeiro colocado;
- calibracao dos intervalos;
- estabilidade da previsao ao adicionar novas pesquisas.

## Avaliacao cronologica

Esta avaliacao responde diretamente: se o projeto existisse antes de cada eleicao historica, o que ele teria previsto usando apenas eleicoes anteriores? Com a coleta de 2006, agora tambem entram testes para 2010 e 2014, antes dos testes principais de 2018 e 2022.

```{python}
#| label: rolling-modelos
#| fig-cap: "Backtest cronologico: treina no passado e prevê a eleicao seguinte"
if rolling_summary.empty:
    fig, ax = plt.subplots()
    ax.text(0.5, 0.5, "Sem avaliacao cronologica ainda", ha="center", va="center", fontsize=14)
    ax.axis("off")
else:
    tmp = rolling_summary.copy()
    tmp["ano_teste"] = pd.to_numeric(tmp["ano_teste"])
    tmp["mae"] = pd.to_numeric(tmp["mae"])
    years = list(tmp["ano_teste"].drop_duplicates())
    fig, axes = plt.subplots(1, len(years), figsize=(5 * len(years), 4), squeeze=False)
    for ax, year in zip(axes.flat, years):
        group = tmp[tmp["ano_teste"] == year].sort_values("mae", ascending=False)
        colors = ["#4C78A8" if model == "baseline_pesquisa" else "#A0A7B3" for model in group["modelo"]]
        ax.barh(group["modelo"], group["mae"], color=colors)
        ax.set_title(f"Previsao de {year}")
        ax.set_xlabel("Erro medio absoluto")
        ax.set_ylabel("")
        for i, row in enumerate(group.itertuples()):
            ax.text(row.mae + 0.25, i, f"{row.mae:.1f}", va="center", fontsize=9)
    plt.tight_layout()
```

```{python}
#| label: rolling-tabela
if not rolling.empty:
    view = rolling.copy()
    view["previsto"] = pd.to_numeric(view["previsto"]).round(2)
    view["resultado_real_validos"] = pd.to_numeric(view["resultado_real_validos"]).round(2)
    view["erro_abs"] = pd.to_numeric(view["erro_abs"]).round(2)
    display(view[["ano_eleicao", "ano_treino_max", "modelo", "cenario_rotulo", "candidato", "previsto", "resultado_real_validos", "erro_abs"]].sort_values(["ano_eleicao", "modelo", "erro_abs"]).head(60))
```

```{python}
#| label: rolling-vencedor
if not rolling.empty:
    view = rolling.copy()
    view["previsto"] = pd.to_numeric(view["previsto"], errors="coerce")
    view["resultado_real_validos"] = pd.to_numeric(view["resultado_real_validos"], errors="coerce")
    predicted = (
        view.sort_values("previsto", ascending=False)
        .groupby(["ano_eleicao", "modelo"], as_index=False)
        .first()[["ano_eleicao", "modelo", "candidato", "previsto"]]
        .rename(columns={"candidato": "vencedor_previsto", "previsto": "voto_previsto"})
    )
    actual = (
        view.sort_values("resultado_real_validos", ascending=False)
        .groupby("ano_eleicao", as_index=False)
        .first()[["ano_eleicao", "candidato", "resultado_real_validos"]]
        .rename(columns={"candidato": "vencedor_real", "resultado_real_validos": "voto_real"})
    )
    mae = view.groupby(["ano_eleicao", "modelo"], as_index=False)["erro_abs"].mean().rename(columns={"erro_abs": "mae"})
    summary = predicted.merge(actual, on="ano_eleicao", how="left").merge(mae, on=["ano_eleicao", "modelo"], how="left")
    summary["acertou_vencedor"] = summary["vencedor_previsto"].eq(summary["vencedor_real"]).map({True: "sim", False: "nao"})
    keep = ["baseline_pesquisa", "random_forest_07_600", "gradient_boosting", "voting_ia", "mlp_pequena", "bayesiano_dinamico_kalman"]
    summary = summary[summary["modelo"].isin(keep)]
    for col in ["voto_previsto", "voto_real", "mae"]:
        summary[col] = pd.to_numeric(summary[col], errors="coerce").round(2)
    display(summary.sort_values(["ano_eleicao", "mae"])[["ano_eleicao", "modelo", "vencedor_previsto", "voto_previsto", "vencedor_real", "voto_real", "acertou_vencedor", "mae"]])
```

## Onde os modelos erram

O erro medio geral e util para ranquear modelos, mas ele esconde duas coisas importantes: eleicoes dificeis e candidatos que os modelos tendem a superestimar ou subestimar. Abaixo, o mesmo backtest cronologico e aberto por ano e por candidato.

```{python}
#| label: rolling-erro-eleicao-modelo
#| fig-cap: "Erro absoluto medio por modelo e eleicao no backtest cronologico"
if rolling.empty:
    fig, ax = plt.subplots()
    ax.text(0.5, 0.5, "Sem erros por eleicao ainda", ha="center", va="center", fontsize=14)
    ax.axis("off")
else:
    view = rolling.copy()
    view["erro_abs"] = pd.to_numeric(view["erro_abs"], errors="coerce")
    keep = ["baseline_pesquisa", "gradient_boosting", "random_forest_07_600", "voting_ia", "mlp_pequena"]
    view = view[view["modelo"].isin(keep)]
    heat = view.pivot_table(index="modelo", columns="ano_eleicao", values="erro_abs", aggfunc="mean")
    heat = heat.loc[[m for m in keep if m in heat.index]]
    fig, ax = plt.subplots(figsize=(9, 4))
    image = ax.imshow(heat, cmap="YlOrRd", aspect="auto")
    ax.set_xticks(range(len(heat.columns)), heat.columns)
    ax.set_yticks(range(len(heat.index)), heat.index)
    ax.set_xlabel("Eleicao prevista")
    ax.set_ylabel("")
    for y, model in enumerate(heat.index):
        for x, year in enumerate(heat.columns):
            value = heat.loc[model, year]
            ax.text(x, y, f"{value:.1f}", ha="center", va="center", fontsize=9)
    fig.colorbar(image, ax=ax, label="Erro medio absoluto")
    plt.tight_layout()
```

```{python}
#| label: rolling-erro-candidato
if not rolling.empty:
    view = rolling.copy()
    view["erro_abs"] = pd.to_numeric(view["erro_abs"], errors="coerce")
    keep = ["baseline_pesquisa", "gradient_boosting", "random_forest_07_600", "voting_ia", "mlp_pequena"]
    view = view[view["modelo"].isin(keep)]
    by_candidate = (
        view.groupby(["modelo", "candidato"], as_index=False)
        .agg(
            eleicoes=("ano_eleicao", "nunique"),
            erro_medio=("erro_abs", "mean"),
            pior_erro=("erro_abs", "max"),
        )
    )
    for col in ["erro_medio", "pior_erro"]:
        by_candidate[col] = by_candidate[col].round(2)
    display(by_candidate.sort_values(["erro_medio", "pior_erro"], ascending=False).head(35))
```

```{python}
#| label: rolling-piores-erros
if not rolling.empty:
    view = rolling.copy()
    view["previsto"] = pd.to_numeric(view["previsto"], errors="coerce").round(2)
    view["resultado_real_validos"] = pd.to_numeric(view["resultado_real_validos"], errors="coerce").round(2)
    view["erro_abs"] = pd.to_numeric(view["erro_abs"], errors="coerce").round(2)
    keep = ["baseline_pesquisa", "gradient_boosting", "random_forest_07_600", "voting_ia", "mlp_pequena"]
    view = view[view["modelo"].isin(keep)]
    cols = ["ano_eleicao", "modelo", "candidato", "previsto", "resultado_real_validos", "erro_abs"]
    display(view.sort_values("erro_abs", ascending=False)[cols].head(30))
```

## Previsao de 2026 por modelo

Depois da avaliacao historica, os mesmos modelos sao treinados com todas as eleicoes disponiveis ate 2022 e aplicados aos cenarios principais de 2026.

```{python}
#| label: rolling-2026-modelos
#| fig-cap: "Previsao de 2026 por modelo, treinada com eleicoes ate 2022"
if rolling_2026.empty:
    fig, ax = plt.subplots()
    ax.text(0.5, 0.5, "Sem previsao 2026 por modelo ainda", ha="center", va="center", fontsize=14)
    ax.axis("off")
else:
    tmp = rolling_2026.copy()
    tmp["previsto"] = pd.to_numeric(tmp["previsto"])
    scenario_options = tmp["cenario_rotulo"].drop_duplicates()
    recent = [s for s in scenario_options if str(s) == CURRENT_SCENARIO_LABEL]
    if not recent:
        recent = [s for s in scenario_options if "agosto/setembro" in str(s) and "Marcal" not in str(s) and "Marçal" not in str(s)]
    first_scenario = recent[0] if recent else scenario_options.iloc[0]
    plot = tmp[tmp["cenario_rotulo"] == first_scenario]
    pivot = plot.pivot_table(index="candidato", columns="modelo", values="previsto", aggfunc="mean")
    if "baseline_pesquisa" in pivot.columns:
        order_candidates = pivot["baseline_pesquisa"].sort_values(ascending=False).head(8).index
        pivot = pivot.loc[order_candidates]
    keep_models = [m for m in ["baseline_pesquisa", "random_forest_07_600", "gradient_boosting", "voting_ia", "mlp_pequena"] if m in pivot.columns]
    if keep_models:
        pivot = pivot[keep_models]
    order = pivot.get("baseline_pesquisa", pivot.mean(axis=1)).sort_values(ascending=True).index
    pivot = pivot.loc[order]
    ax = pivot.plot(kind="barh", figsize=(10, 6))
    ax.set_title(first_scenario)
    ax.set_xlabel("Votos validos estimados (%)")
    ax.set_ylabel("")
    ax.legend(title="Modelo", bbox_to_anchor=(1.02, 1), loc="upper left")
    plt.tight_layout()
```

```{python}
#| label: rolling-2026-tabela
if not rolling_2026.empty:
    view = rolling_2026.copy()
    view["previsto"] = pd.to_numeric(view["previsto"]).round(2)
    scenario_options = view["cenario_rotulo"].drop_duplicates()
    recent = [s for s in scenario_options if str(s) == CURRENT_SCENARIO_LABEL]
    if not recent:
        recent = [s for s in scenario_options if "agosto/setembro" in str(s) and "Marcal" not in str(s) and "Marçal" not in str(s)]
    first_scenario = recent[0] if recent else scenario_options.iloc[0]
    focus = view[view["cenario_rotulo"].eq(first_scenario)].copy()
    top_candidates = (
        focus[focus["modelo"].eq("baseline_pesquisa")]
        .sort_values("previsto", ascending=False)
        .head(8)["candidato"]
    )
    focus = focus[focus["candidato"].isin(top_candidates)]
    keep_models = ["baseline_pesquisa", "random_forest_07_600", "gradient_boosting", "voting_ia", "mlp_pequena"]
    focus = focus[focus["modelo"].isin(keep_models)]
    display(focus[["modelo", "cenario_rotulo", "candidato", "previsto", "ano_treino_max"]].sort_values(["candidato", "modelo"]))
```

```{python}
#| label: comparacao-modelos
#| fig-cap: "Erro medio absoluto no backtest por modelo"
if backtest.empty:
    modelos = pd.DataFrame({
        "modelo": ["Baseline", "Inferencial", "ML tradicional", "IA/rede neural", "Ensemble"],
        "entra_agora": ["Sim", "Parcial", "Depois", "Depois", "Depois"],
        "papel": [
            "Primeira previsao viva",
            "Entender impacto das variaveis",
            "Tentar reduzir erro fora da amostra",
            "Capturar interacoes se houver dados",
            "Combinar apenas modelos aprovados",
        ],
    })
    display(modelos)
else:
    tmp = backtest.copy()
    tmp["mae"] = pd.to_numeric(tmp["mae"])
    summary = tmp.groupby("modelo", as_index=False)["mae"].mean().sort_values("mae")
    fig, ax = plt.subplots()
    ax.barh(summary["modelo"], summary["mae"], color="#4C78A8")
    ax.invert_yaxis()
    ax.set_xlabel("Erro medio absoluto")
    ax.set_ylabel("Modelo")
    ax.set_title("Backtest: menor erro e melhor")
    for i, row in enumerate(summary.itertuples()):
        ax.text(row.mae + 0.15, i, f"{row.mae:.2f}", va="center", fontsize=9)
    plt.tight_layout()
```

## Backtest ponderado pela qualidade da base

O ranking bruto trata 1994, 1998 e 2022 como se tivessem a mesma confiabilidade de medicao. O ranking ponderado abaixo reduz o peso de anos com pouca cobertura, poucos institutos, muita amostra imputada ou dependencia forte de base agregada.

```{python}
#| label: pesos-qualidade-backtest
if not polls.empty and not backtest.empty:
    base = polls.copy()
    unique = base.drop_duplicates(["ano_eleicao", "instituto", "data_publicacao", "cenario"]).copy()
    unique["amostra_imputada"] = pd.to_numeric(unique.get("amostra_imputada", 0), errors="coerce").fillna(0)

    def categoria_fonte_backtest(value):
        text = str(value).lower()
        if "primaria" in text or "confirmad" in text or "oficial" in text:
            return "fonte forte"
        if "wikipedia" in text or "pesqele" in text or "normalizada" in text:
            return "base agregada"
        if "secundaria" in text or "folha" in text or "gazeta" in text:
            return "fonte secundaria auditada"
        return "outra"

    unique["categoria_fonte"] = unique["qualidade_fonte"].map(categoria_fonte_backtest)
    weights = (
        unique.groupby("ano_eleicao")
        .agg(
            pesquisas_cenarios=("cenario", "size"),
            institutos=("instituto", "nunique"),
            base_agregada_pct=("categoria_fonte", lambda s: s.eq("base agregada").mean() * 100),
            amostra_imputada_pct=("amostra_imputada", lambda s: s.mean() * 100),
        )
        .reset_index()
    )
    penalty = (
        weights["pesquisas_cenarios"].lt(10).astype(float) * 0.20
        + weights["institutos"].lt(3).astype(float) * 0.15
        + weights["base_agregada_pct"].gt(50).astype(float) * 0.20
        + weights["amostra_imputada_pct"].gt(50).astype(float) * 0.15
    )
    weights["peso_qualidade_base"] = (1 - penalty).clip(lower=0.40).round(2)
    display(weights.sort_values("ano_eleicao"))
```

```{python}
#| label: ranking-modelos-ponderado
if not polls.empty and not backtest.empty:
    view = backtest.copy()
    view["mae"] = pd.to_numeric(view["mae"], errors="coerce")
    view = view.merge(weights[["ano_eleicao", "peso_qualidade_base"]], left_on="ano_teste", right_on="ano_eleicao", how="left")
    view["peso_qualidade_base"] = view["peso_qualidade_base"].fillna(1.0)
    view["mae_vezes_peso"] = view["mae"] * view["peso_qualidade_base"]
    weighted = (
        view.groupby("modelo", as_index=False)
        .agg(mae_vezes_peso=("mae_vezes_peso", "sum"), peso_total=("peso_qualidade_base", "sum"))
    )
    weighted["mae_ponderado"] = weighted["mae_vezes_peso"] / weighted["peso_total"]
    weighted = weighted[["modelo", "mae_ponderado"]]
    raw = view.groupby("modelo", as_index=False)["mae"].mean().rename(columns={"mae": "mae_bruto"})
    shown = raw.merge(weighted, on="modelo")
    shown["delta"] = shown["mae_ponderado"] - shown["mae_bruto"]
    for col in ["mae_bruto", "mae_ponderado", "delta"]:
        shown[col] = shown[col].round(2)
    display(shown.sort_values("mae_ponderado").head(12))
```

```{python}
#| label: tabela-backtest
if not backtest.empty:
    view = backtest.copy()
    view["mae"] = pd.to_numeric(view["mae"])
    top = view.groupby("modelo")["mae"].mean().sort_values().head(8).index
    display(view[view["modelo"].isin(top)].sort_values(["ano_teste", "mae"]))
```

```{python}
#| label: backtest-janelas
#| fig-cap: "Erro do baseline por dias antes da eleicao"
if backtest_janelas.empty:
    fig, ax = plt.subplots()
    ax.text(0.5, 0.5, "Sem backtest por janela ainda", ha="center", va="center", fontsize=14)
    ax.axis("off")
else:
    tmp = backtest_janelas.copy()
    tmp["dias_antes_eleicao"] = pd.to_numeric(tmp["dias_antes_eleicao"])
    tmp["mae"] = pd.to_numeric(tmp["mae"])
    tmp["modelo"] = tmp.get("modelo", "baseline_pesquisa")
    summary = tmp.groupby(["modelo", "dias_antes_eleicao"], as_index=False)["mae"].mean().sort_values("dias_antes_eleicao")
    fig, ax = plt.subplots()
    for model, group in summary.groupby("modelo"):
        ax.plot(group["dias_antes_eleicao"], group["mae"], marker="o", label=model)
    ax.invert_xaxis()
    ax.set_xlabel("Dias antes da eleicao")
    ax.set_ylabel("Erro medio absoluto")
    ax.set_title("Quanto mais perto da eleicao, menor tende a ser o erro")
    ax.legend()
    plt.tight_layout()
```

```{python}
#| label: backtest-previsao-viva
#| fig-cap: "Simulacao de previsao viva: usa apenas pesquisas publicadas ate cada corte"
if backtest_vivo.empty:
    fig, ax = plt.subplots()
    ax.text(0.5, 0.5, "Sem backtest de previsao viva ainda", ha="center", va="center", fontsize=14)
    ax.axis("off")
else:
    tmp = backtest_vivo.copy()
    tmp["dias_antes_eleicao"] = pd.to_numeric(tmp["dias_antes_eleicao"])
    tmp["mae"] = pd.to_numeric(tmp["mae"])
    tmp["modelo"] = tmp.get("modelo", "baseline_pesquisa")
    summary = tmp.groupby(["modelo", "dias_antes_eleicao"], as_index=False)["mae"].mean().sort_values("dias_antes_eleicao")
    fig, ax = plt.subplots()
    for model, group in summary.groupby("modelo"):
        ax.plot(group["dias_antes_eleicao"], group["mae"], marker="o", label=model)
    ax.invert_xaxis()
    ax.set_xlabel("Dias antes da eleicao")
    ax.set_ylabel("Erro medio absoluto")
    ax.set_title("Previsao viva melhora conforme novas pesquisas ficam disponiveis")
    ax.legend()
    plt.tight_layout()
```

## Proximas entregas

1. Auditar amostra das pesquisas extraidas automaticamente.
2. Buscar series completas de 2002, 1998 e 1994 no Banco de Dados Datafolha/CESOP ou em acervos dos institutos.
3. Melhorar o Bayesiano dinamico com efeito de instituto e fundamentos como prior.
4. Buscar series nacionais equivalentes para 1998 e 1994.
5. Adicionar aprovacao do governo, rejeicao e renda real.
